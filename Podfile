# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'awesomPhone' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for awesomPhone
  pod 'Alamofire', '~> 4'
  pod 'ReSwift', '~> 4'
  pod 'ObjectMapper', '~> 3'
  pod 'Kingfisher', '~> 4'

end
